services:
  - type: web
    name: kinopub-4k-proxy
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60
    plan: free
    autoDeploy: false
